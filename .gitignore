# Secrets — never commit (CLAUDE.md §5)
# .env.example was previously committed with real keys; it is now an untracked,
# local-only placeholder template. Both .env and .env.example stay ignored.
.env
.env.*
.claude.json
.claude/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Raw corpora are never committed (PRD §0.5)
data/
*.parquet
*.jsonl

# Generated run artifacts (Context Packs, run logs)
*.log
pack.json
pack.md
sweep_pack.json
sweep_pack.md

# HF / model caches
.cache/

# OS
.DS_Store
Thumbs.db
